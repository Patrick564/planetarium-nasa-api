import Input from './Input.js'
import Select from './Select.js'

const Form = ({ form, ...props }) => {
  return (
    <div>
      <form onSubmit={form}>

      </form>
    </div>
  )
}

export default Form
